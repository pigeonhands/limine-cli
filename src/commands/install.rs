use crate::{LimeineCliError, Result, cli::InstallArgs, config::Config};

static LIMEINE_DATA_DIR: &str = "/usr/share/limine";

pub fn run(config: &Config, args: &InstallArgs) -> Result<()> {
    Ok(())
}
